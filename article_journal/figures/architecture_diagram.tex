\begin{figure}[!htbp]
\centering
\begin{tikzpicture}[
  font=\small,
  block/.style={draw, rounded corners, align=center, minimum width=26mm, minimum height=9mm, fill=mralight},
  narrow/.style={draw, rounded corners, align=center, minimum width=22mm, minimum height=9mm, fill=mralight},
  store/.style={draw, align=center, minimum width=26mm, minimum height=9mm, fill=white},
  ctrl/.style={draw, rounded corners, align=center, minimum width=26mm, minimum height=9mm, fill=white, line width=0.9pt},
  arrow/.style={-{Latex[length=2mm]}, thick},
  birarrow/.style={{Latex[length=2mm]}-{Latex[length=2mm]}, thick}
]
% top configuration pipeline
\node[store]  (track)   at (0,5.4)   {Track/race configuration};
\node[block]  (loader)  at (3.3,5.4) {Configuration loader\\\scriptsize + validator};
\node[block]  (builder) at (6.6,5.4) {Arena builder\\\scriptsize scenario};

% runtime row
\node[block]  (runner)  at (6.6,3.5) {Race runner};
\node[narrow] (adapter) at (2.2,3.5) {Simulator adapter};
\node[narrow] (referee) at (11.4,3.5){Referee\\\scriptsize evaluation};
\node[block]  (logs)    at (11.4,1.5){Outputs\\\scriptsize summary, event logs};

% control-loop row (official side)
\node[block]  (obs)        at (6.6,1.5) {Participant information};
\node[ctrl]   (controller) at (2.7,1.5) {Participant controller};

% configuration edges
\draw[arrow] (track) -- (loader);
\draw[arrow] (loader) -- (builder);
\draw[arrow] (builder) -- (runner);

% runtime edges
\draw[birarrow] (runner) -- node[above, pos=0.5, align=center, font=\scriptsize]{runtime data\\and tick} (adapter);
\draw[arrow]    (runner) -- node[above, font=\scriptsize]{evaluation data} (referee);
\draw[arrow]    (referee) -- (logs);

% control loop: runner -> obs -> controller -> runner
\draw[arrow] (runner) -- node[right, font=\scriptsize]{observation} (obs);
\draw[arrow] (obs) -- (controller);
\draw[arrow] (controller.north) -- node[above left=-1mm and -1mm, font=\scriptsize]{vehicle command} (runner.south west);

% official-observation trust boundary
\begin{scope}[on background layer]
  \node[draw, dashed, rounded corners, line width=0.8pt, fill=mralight!45,
        fit=(controller)(obs), inner sep=4mm] (tb) {};
\end{scope}
\node[anchor=north, font=\scriptsize\itshape] at (tb.south)
     {participant interface};
\end{tikzpicture}
\caption{Marine Race Arena runtime architecture.}
\label{fig:runtime_architecture}
\end{figure}
