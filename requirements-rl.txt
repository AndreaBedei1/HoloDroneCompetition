# Marine Race Arena - dependencies for the learned reference controller.
#
# These are NOT part of requirements.txt and must NOT be installed into the
# canonical benchmark environment, so the environment that produced the
# reported benchmark results stays unchanged. Install them into a dedicated
# environment, on top of the benchmark stack (HoloOcean 2.3.0 client +
# requirements.txt):
#     pip install -r requirements.txt
#     pip install -r requirements-rl.txt
#
# Verified together with Python 3.9.25 and numpy==2.0.2 (the benchmark pin).
# They are required to load and evaluate the released checkpoint
# artifacts/paper/ppo/model/policy_recurrent_ppo_27d.zip.

gymnasium==1.0.0           # step-wise environment API
torch==2.8.0               # policy network (CPU wheel; a CUDA build also works)
stable-baselines3==2.7.1   # policy and feature-extractor base classes
sb3-contrib==2.7.1         # RecurrentPPO, the architecture of the released policy
