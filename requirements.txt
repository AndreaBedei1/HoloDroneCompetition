# Marine Race Arena - core benchmark runtime.
# Verified with Python 3.9 in a conda environment named "ocean".
#
# IMPORTANT: HoloOcean 2.3.0 is NOT on PyPI and is therefore NOT installed by
# this file. `pip install holoocean==2.3.0` fails (PyPI only ships 0.5.8).
# Install the HoloOcean 2.3.0 client from its official source *before* this file:
#     cd <HoloOcean-2.3.0 source>/client && pip install .
# The client pulls in numpy, scipy and matplotlib unpinned; the numpy pin below
# then fixes the exact version used for the reported results. See the README
# "Installation" section and https://byu-holoocean.github.io/holoocean-docs.
#
# After the client and the pins below are installed, HoloOcean still needs its
# simulator world package ("Ocean") downloaded once:
#     python -c "import holoocean; holoocean.install('Ocean')"
# Verify with:
#     python -c "import holoocean; print(holoocean.installed_packages())"   # -> ['Ocean']

numpy==2.0.2               # pin the numpy that the HoloOcean client pulls in unpinned

# Onboard gate detector and the optional front-camera preview.
opencv-python==4.12.0.88

# Manual controllers used for inspection and data collection.
pygame==2.6.1

# Optional, imported lazily: verifies that a HoloOcean engine process is gone
# after a run on Windows. Without it the adapter logs and continues, so the
# version is deliberately not pinned - it cannot affect a reported result.
psutil>=5.9
